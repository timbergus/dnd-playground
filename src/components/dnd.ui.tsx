import styled from 'styled-components'

export const Handler = styled.div`
  position: absolute;
  top: 4px;
  right: 4px;
  width: 24px;
  height: 32px;
  background-color: red;

  &:hover {
    cursor: grab;
  }
`
